import {Text , View, Box, ScrollView, VStack } from 'native-base'
import React from 'react'
import Colors from "../../color"


function Orders(){
    return (
        <Box h="full" bg={Colors.white} px={5}>
            <ScrollView showsVerticalScrollIndicator={false}>

            </ScrollView>
        </Box>
    )
}
export default Orders
